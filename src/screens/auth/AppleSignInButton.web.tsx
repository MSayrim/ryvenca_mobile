/** Web preview: Sign in with Apple is native-only here (never import expo-apple-authentication on web). */
export function AppleSignInButton(_props: { onPress: () => void; loading?: boolean; disabled?: boolean }) {
  return null;
}
